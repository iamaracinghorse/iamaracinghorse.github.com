# Copy to .env.local and fill in values

# Skip Apple Music auth (for demo / local dev without a developer account)
# VITE_BYPASS_AUTH=true

# Embed your MusicKit Developer Token so users don't need to enter it.
# Generate at: https://developer.apple.com → Keys → MusicKit
# VITE_MUSICKIT_TOKEN=eyJhbGci...
